# The-string-can-be-traversed-using-for-loop  

Python program
    msg = 'Good bye'
    for index in range(0,len(msg));
         letter = msg[index]
         print(letter)
         index = index + 1    
